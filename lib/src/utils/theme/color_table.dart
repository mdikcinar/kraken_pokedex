import 'package:flutter/material.dart';

class ColorTable {
  static const textColor = Colors.black87;
  static const darkThemeTextColor = Colors.white;
  static const primaryColor = Color.fromARGB(255, 240, 99, 146);
  static const primaryColorLight = Color.fromRGBO(254, 221, 229, 1);
}
